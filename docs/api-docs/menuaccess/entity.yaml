/menuaccess/{id}:
  get:
    tags:
      - MenuAccess
    description:
      entity.
    parameters:
      - name: id
        in: path
        required: true
        type: string
    responses:
        200:
            description: "status:1 is success; status: 0 is failure;"
